import 'package:flutter/material.dart';

class MapTSP extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      appBar: AppBar(
        title: Text('Map'),
        backgroundColor: Colors.lightGreen,
      ),
    );
  }
}